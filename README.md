# 救一笔！

只准补一笔的椅子急救小游戏。**47 个完整中文关卡：保留原13关、6关难易交错的「脑洞接力」、渐进课程前三批 15 关（当前 15/50），以及 13 个「派生试玩」主配置（原3个+前两批5个+再来5个，含新机制：挂点定时松手、弹性落球、触碰触发平台与低摩擦地形）**，真实二维刚体物理，鼠标 / 触屏 / 键盘可玩；无后台、无 CDN、无联网 AI。每关都是独立、可序列化的 JSON 文件，游戏内可一键导出。

## 立即试玩

在本目录打开终端（Node.js 18+）：

```sh
npm start
```

打开 **http://127.0.0.1:4173/**。运行游戏无需 `npm install`。

新内容直接玩 **http://127.0.0.1:4173/#level=lab%2Fup-we-go**，或点击「脑洞接力」。原13关、数字链接和成绩均保留；新关重点是电梯接力、独立挡板、重力旅行等新因果，不只挤障碍。

渐进课程（建设中，前三批 15/50）从 **http://127.0.0.1:4173/#level=course%2F01-reach-the-ground** 或「课程一·承重与重心」开始；设计卡、章节分配与验收记录见 **[docs/COURSE_50.md](docs/COURSE_50.md)**。首批 5 关试玩反馈「过于简单」后，06–10 已按「直觉补腿必然失败」重新设计并留有多条宽松路线；11–15 以真实椅型结构为题（悬臂/高背/摇椅/郁金香/跪坐），4 关有核实原型，通关后来源条展示原型名、署名与可点击出处链接（仅点击才访问外站）。

「派生试玩」章节（原3个/前两批5个/再来5个，共 13 个主配置）从 **http://127.0.0.1:4173/#level=var2%2Fv1-ballast** 或章节页签进入：双向阵风、偏心撤板挂接、左移出生点导流、配重球、挂点定时松手、双平台接力、重力去程回程、弹性球导流；均为经同引擎 cases 验收的独立关卡。「派生试玩 · 再来5个」从 **http://127.0.0.1:4173/#level=var3%2Fw1-tailwind** 进入：顺丰便车、一笔两职（腿兼挡板）、球来开电梯（触碰触发平台）、第二球不走旧路、弹球收纳箱（低摩擦冰面）。设计方法沉淀（角色反转、关系/时序变化、一笔多职、可观察因果、保留替代解等）见 **[docs/LEVEL_DESIGN.md](docs/LEVEL_DESIGN.md)**。

- 端口被占用：`npm start -- --port 4174`。
- 不依附终端的后台服务：`npm run start:background`。该命令健康检查通过后返回 URL / PID，使用 `detached`、独立标准流及 `unref`，状态保存在 `.local/server.json`。若 IDE 使用全进程树清理策略导致服务停止，重新运行 `npm start` 即可。
- 前台服务按 `Ctrl+C` 停止；后台服务可按 `.local/server.json` 中的 PID 在任务管理器结束。
- 也可先 `npm run content:build`，再用 `python -m http.server 4173 --bind 127.0.0.1` 启动纯静态目录；新增文件后须重新构建索引。
- 只监听本机回环地址，不是公网发布。挑战链接携带 `#level=关卡号`；本机链接不能直接给其他设备访问。

## 怎么玩

1. 看本关试坐条件，在**本关亮色标明的画区**按住鼠标 / 一根手指画一条连续线；入门默认椅面以下，部分变种开放上方或只准修上面。
2. 墨水按实际轨迹长度扣除。一笔可以拐弯；用尽会自动收笔。靠近木椅水平面、地板或台阶顶面的端点有 10 个画布单位的轻微吸附，吸附仍计费。
3. 碰到木椅的线会焊成硬支架；没有接上的线是自由落体，绝不钉在空气里。要把重量传到实心表面，或接上本关明确标出的墙面金环；不能钻入实体、猫区或落球出生圈。
4. 抬笔后点击「请阿稳试坐」，观察 5 秒。实际接触点显示绿色箭头，重量投影也可观察。
5. 随时清除 / 立即重试，不扣命。两级提示不扣星，任意选关；成功后可继续或挑战少墨水纪录。

通过即至少一星；用墨 ≤ 本关三星目标获三星，其余 ≤ 墨水总量 91% 获两星。成绩和音效设置仅保存在本机 `localStorage`，拒绝存储时仍可正常玩。

键盘：`Tab` 选中画板，方向键移动笔尖，空格落笔 / 抬笔，`Shift` 慢移；`R` 重画，`Enter` 试坐。画板外可正常触屏滚动。音效默认关闭，可手动开启。

## 原有十三个小谜题

| 关卡 | 智力点 | 墨水 / 三星目标 |
| --- | --- | --- |
| 少了一条腿 | 接椅子、找地面 | 185 / 145 |
| 胖橘偏心 | 载荷偏右，扩大支撑范围 | 185 / 150 |
| 地板也请假 | 洞不承重，斜跨到实心地板 | 185 / 157 |
| 别吵醒老板 | 一笔转弯，避开整片睡猫领地 | 235 / 205 |
| 借个台阶 | 墨水不够到地板，借台阶顶面 | 103 / 82 |
| 无腿自由人 | 两条旧腿全无，一笔同时造两脚 | 345 / 310 |
| 左右为难 | 两次方向相反的定时横向推力，两边都要抗风 | 480 / 432 |
| 高低双岛 | 没有旧腿、仅有两块不同高度的小平台 | 412 / 365 |
| 双猫夹击 | 双禁区窄通道，折线绕行后还要跨过更宽的坑 | 335 / 302 |
| 支点也下班 | 2.2秒撤走临时踏板，开局稳定不等于最终稳定 | 190 / 160 |
| 借个天花板 | 金环可转铰接，真的能悬挂；一个偏心挂点会摆转 | 500 / 450 |
| 地板拐弯了 | 2秒后重力向右，右墙变成新的承重面 | 450 / 405 |
| 别只会补腿 | 上方施工，真球会下落滚动；平顶接球会翻，斜顶可导走 | 330 / 295 |

## 脑洞接力：轻松与烧脑交错

| 稳定 id | 新关 | 认知惊喜 / 难度 |
| --- | --- | --- |
| `lab/up-we-go` | 一起坐电梯 | 两边同乘一个上升平台 / 轻松 |
| `lab/last-stop` | 脚下的班车 | 横梁让接触点滑动换班，不追着平台造腿 / 烧脑 |
| `lab/off-duty` | 地板先下班 | 装卸踏板撤走，双挂点接管重量 / 轻松 |
| `lab/world-tour` | 下方环球旅行 | 下→左→上，双挂点约束而不是万能胶 / 烧脑 |
| `lab/not-a-chair-leg` | 这一笔不修椅子 | 独立挂起的斜挡板把球导离猫，根本不焊椅子 / 轻松 |
| `lab/return-ticket` | 别忘了回程 | 重力回向下时侧墙也退场，还要有永久落脚点 / 推理 |

### 课程（建设中）：第一章·承重与重心 10/10

| 稳定 id | 课程关 | 教学点 / 难度 |
| --- | --- | --- |
| `course/01-reach-the-ground` | 左腿请假了 | 一笔要碰到木椅并落到地面 / 轻松 |
| `course/02-start-from-ground` | 从地面往上接 | 起笔方向自由，从地到椅同样焊牢 / 轻松 |
| `course/03-not-welded` | 差一点点没碰上 | 没焊上的笔画救不了椅子 / 轻松 |
| `course/04-borrow-the-leg` | 旧脚别浪费 | 从已有椅脚接着画，旧脚传重 / 轻松 |
| `course/05-full-route` | 新工位，一条腿都没有 | 一笔走完「地→椅→地」完整承重路线 / 推理 |
| `course/06-follow-the-weight` | 人往哪边坐，支架往哪边偏 | 支撑范围跟重量投影，不对齐椅面中心 / 推理 |
| `course/07-two-kinds-of-cat` | 两只猫，两种愁 | 抱猫是重量、睡猫是禁区，躲猫也要撑住重量 / 推理 |
| `course/08-two-short-legs` | 两只脚都够不着 | 一笔横档把两只悬空旧脚都接到地 / 烧脑 |
| `course/09-wide-enough-span` | 别都挤到一边 | 双脚分居重量两侧，躲过禁区也不能缩一边 / 推理 |
| `course/10-high-low-feet` | 一脚高，一脚低 | 台阶落脚面不够宽，高低两脚才稳住 / 烧脑 |

第二章「空间与外力」进行中，椅型结构成为谜题本体（11–15 已实施）：

| 稳定 id | 课程关 | 原型 / 结构挑战 / 难度 |
| --- | --- | --- |
| `course/11-cantilever-rail` | 悬臂椅 · 前排脚下是空的 | 悬臂椅（Mart Stam 等，1920年代末）/ 底轨要接过地板洞，竖直伸腿会踩空 / 推理 |
| `course/12-tall-back` | 高背椅 · 往后倒 | 原创结构（Z 字斜撑在矩形契约内不忠实，未硬凑原型）/ 高背压后，判断倾倒方向 / 烧脑 |
| `course/13-rocking-runner` | 摇椅别摇 | 摇椅（题图为 Thonet 曲木摇椅）/ 三阶离散弧轨窄接触，底轨要往重量那侧加长 / 轻松 |
| `course/14-tulip-base` | 郁金香椅 · 底座不够大 | 郁金香椅（Eero Saarinen，1955–1956，Knoll）/ 单柱窄底座偏心承重，借台阶落脚 / 推理 |
| `course/15-kneeling-two-points` | 跪坐椅 · 前低后高都悬空 | Balans 跪坐椅（Hans Christian Mengshoel，1979）/ 相连的高低双支点要一笔都接地 / 烧脑 |

这些关卡为受结构启发的二维虚构谜题（直角矩形近似，简化说明随关展示）；4/5 关有核实过的真实原型，通关后画布下方来源条展示原型与出处，来源清单与核实记录见 docs/COURSE_50.md。

课程总计规划 5 章 50 关，当前完成前三批共 15 关技术验证（06–10 按首批试玩反馈提高难度，11–15 接入椅型原型与来源展示），后续关卡与难度校准等待试玩反馈；索引见 [docs/COURSE_50.md](docs/COURSE_50.md)。

### 动态添加，不手写目录

1. 在自己的 `levels/<namespace>/<slug>.json` 增加 v3 关卡，填写稳定id与 progression；在 `tests/cases/<namespace>/` 配合法解和失败例。
2. 用 `npm start` 开发服务时，点击「刷新目录」即可发现新增文件，无需重启；坏文件显示路径并隔离，不破坏当前笔画、试坐、成绩或导入库。
3. 新类型放在 `js/modules/<namespace>/<slug>.js` 自注册；场景配方与模块接口见 **[docs/MODULES.md](docs/MODULES.md)**，不用编辑通用主循环或中心 imports。
4. 纯静态发布前运行 `npm run content:build`，然后 `npm run content:check`，一起发布文件与生成索引。Nginx 不扫描目录，刷新不会从 GitHub 拉未发布内容，也不需要生产 Node 服务。

本轮未提交、推送或部署。难度是作者标注，合法解不等于趣味性已经由玩家确认。

## 单关文件与序列化

完整坐标/字段规则、引擎机制边界、可直接使用的 JSON 示例、离线流程与 LLM 提示见 **[docs/LEVEL_FORMAT.md](docs/LEVEL_FORMAT.md)**；机器可读格式见 **[v1 Schema](schemas/level-v1.schema.json)** / **[v2 规则变种 Schema](schemas/level-v2.schema.json)** / **[v3 模块组合 Schema](schemas/level-v3.schema.json)**。

点击「导入 / 本地关卡」导入 UTF-8 JSON：校验成功后自动存入本机，刷新后可从本地列表继续玩。最多12份、每份64 KiB；同 id 不覆盖内置关，同内容去重，坏文件/保存失败不会切换关卡或覆盖旧存档。移除本地存档需二次点击确认。

`levels/manifest.json` 是自动生成的发布目录；开发服务用同一个发现器实时返回它。顺序由每关 progression 决定，旧关的编排放在 `levels/legacy.progression.json`；没有 id+1 或必须连续编号的限制。

点击画板标题右侧 **「↓ 保存关卡」**，可下载本关的完整 JSON；内容与项目中的原文件无损一致，包含：

| 字段 | 含义 |
| --- | --- |
| `format` / `version` | `save-one-stroke-level` / `1`、`2`或`3`；旧关保持原规则 |
| `id` / `chapter` | v1/v2数字id不变；v3为namespace/name和自定义分组 |
| `scene` / `progression` / `drawArea` | v3的模块实例、编排和可选画区；替代下列旧版几何与mechanics字段 |
| `title`、`brief`、`condition` 等 | 标题、规则及试坐反馈文字 |
| `ink` / `gold` | 墨水上限、三星用墨目标 |
| `chair[]` | 木椅矩形 `{x,y,w,h,kind}`，第一块必须是 `seat` |
| `terrain[]` | 实际承重矩形 `{x,y,w,h}`，不需要用图片推断 |
| `load` | 人的 `x`、`mass`，以及可选抱猫重量 |
| `forbidden` / `forbiddenZones[]` | 单个 / 多个猫区矩形 |
| `forces[]` | 可选定时外力：`start/end` 毫秒、`fx/fy`、施力点 `at:{x,y}`、显示 `label` |
| `mechanics` | v2必填：`temporaryTerrain`、`anchors`、`gravity`、`drops`规则数组及可选`drawTop/drawBottom`画区 |
| `hint[]` / `labels[]` / `gap` | 提示、画板标注、空洞警示；空洞能否承重仍由 terrain 决定 |

坐标系固定为 **720×480，左上角为原点，y 向下**。JSON 只存关卡，不存执行函数、物理引擎对象或参考答案；玩家成绩仍在 localStorage，避免把关卡与游玩状态混在一起。

可直接导入的例子：`docs/examples/level.json`（v1）、`docs/examples/variant.json`（v2）、`levels/lab/up-we-go.json`（v3）。加载时统一验证格式与参数；v3导出保留配方而不是展开缓存。详细旧版字段和v3差异见 LEVEL_FORMAT.md，不要混用。当前不扩展关卡编辑器或在线模型调用。导出的文件不含玩家解答或成绩；跨设备请发 JSON，而不是只发 `#local=...` 本地引用链接。

## 物理模型与边界

- 本地 **Matter.js 0.20.0**；笔画由真实有质量的线段 / 端点碰撞体组成，木椅与接上的笔画为复合刚体。形状、落点、载荷和障碍都参与计算，不识别预设答案。
- 阿稳是抱住椅子的刚性试坐假人，其重心和重量计入复合刚体；不是独立布娃娃。木头和墨水在本版不会断裂，整个结构会滑动、旋转、翻倒。
- 复合结构用平行轴定理补足转动惯量，避免细长结构不真实的碰撞发射。模拟固定为 120 Hz，浏览器与测试使用完全相同的 `physics.js`。
- 任意时刻倾斜超过 24°、座面下坠超过 65 个单位、移出试坐范围或碰猫，失败不可逆。到 5 秒时还须倾斜 <10°、连续稳定至少 0.7 秒且具有实际承重接触或木椅的有效金环铰接，才会通过。可观察轻微晃动，不是按“碰到两个点”直接宣布成功。
- v2 临时地形会从物理世界移除；金环创建可转动的零长度约束；重力时序改写整个世界方向；落球到时才创建动态圆球。预告、图形与失败原因都读取机制数据，不按关号判定，不强行设静止或识别答案。
- 定时外力通过 Matter 的 `Body.applyForce` 在指定载荷点逐帧施加，真的产生滑动和转矩；不是根据某个画法触发的预设翻倒动画。第 7 关无风时能坐稳的单侧支架，在反向风中会翻倒。
- 轨迹有 1.2 单位的几何简化以降低碰撞体数量，但墨水按原轨迹计费；原轨迹和简化轨迹都会做障碍检查。
- 离开后台标签或打开玩法窗口时暂停试坐，不因掉帧跳过物理过程。

## 验收

无需安装任何依赖即可离线检查单关（不提供 cases 时只代表格式通过，不代表可解）：

```sh
npm run level:validate -- docs/examples/level.json --json
npm run level:validate -- docs/examples/level.json --cases docs/examples/level.cases.json --json
npm run level:validate -- docs/examples/variant.json --cases docs/examples/variant.cases.json --json
```

工程测试需要开发依赖（AJV 只用于 Schema 测试，Playwright 只用于浏览器验收；游戏运行不需要它们）：

```sh
npm ci --cache .cache/npm
npm test
npm run content:check
# 模块组合专项；浏览器脚本需先启动 npm start，可用 GAME_URL 指定地址
npm run test:content
npm run test:content:browser
# 第三轮只验新变种，不重跑旧关矩阵
npm run test:variants
npm run test:variants:browser
# 第二轮的格式 / 本地导入专项入口仍保留
npm run test:format
npm run test:import
```

原有核心测试保留：每关各有一个合法解、一个合法但失败的画法、一个不同路径的合法解；还覆盖悬空自由落体、空洞因果、改变重心、猫区线段穿越、墨水限额、吸附、坐满五秒、端点容错、失败不可逆、JSON 无损往返、文件格式校验、定时反向风的因果和双猫禁区。

第二轮另有 **9 项格式/存储/离线入口测试**，覆盖按版本选择的 Schema、坏文件、配额、id隔离和本地恢复；`test:import` 聚焦原生鼠标导入 → 试坐 → JSON 导出，验证刷新恢复、错误文件和390px布局，不重复完整矩阵。

第三轮 `test:variants` 检查新四关的合法解、另一条自由解、朴素失败、JSON往返和真实机制事件；另将新机制去掉做反事实检查，证明失败确实由规则变化引起。`test:variants:browser` 用原生鼠标验证四种变化及落球失败/快重试，并走通 v2 导入试玩导出和390px布局。

原有全关浏览器入口保留（本轮不重跑旧矩阵，仅开发测试需要 Playwright）：

```sh
npm ci --cache .cache/npm
npm run test:browser
```

测试优先使用本机 Chrome / Edge / Chromium，也可用 `BROWSER_PATH` 指定可执行文件。使用隔离浏览器和项目内临时目录，不读写用户浏览器资料；服务未启动时会临时启动并在测试结束后关闭。

前两轮历史验收覆盖：
- 当时的 **9 关**均通过原生鼠标绘画 → 5 秒试坐 → 成功，**27 星毕业**；难度分组切换、刷新后成绩保留。
- 原生鼠标失败 → 结果 → 一键重试；两级提示、玩法窗和剪贴板实际挑战链接。
- 点击「保存关卡」真实下载 JSON，与原文件做深相等校验。
- 390×844 窄屏通过 Chromium 原生触摸输入绘画 → 通关 → 重试；320px 无横向溢出，操作按钮至少 44px。
- 游戏资源全部同源本地，加载后断开浏览器网络仍能完成九关；Chromium 因切换 hash 重新请求 favicon 而产生的预期离线拒绝单独计数，游戏无运行错误。

截图和机器验收结果：`.local/desktop-ready.png`、`.local/desktop-graduated.png`、`.local/mobile-touch-win.png`、`.local/browser-evidence.json`。可解与失败样例仅位于 `tests/fixtures.cjs`，游戏运行时不加载它。

## 文件

- `index.html` / `styles.css`：纸张风中文界面、响应式布局。
- `levels/**/*.json`：独立版本化关卡；manifest为生成物，不手改。
- `js/levels.js` / `js/catalog.js`：发现、刷新、排序和逐文件错误隔离。
- `js/content.js` / `js/modules/<owner>/`：可信模块注册、元素、规则和场景配方。
- `js/level-format.js` / `js/level-library.js`：闭合格式校验、单关解析与原子本地存档。
- `docs/LEVEL_FORMAT.md` / `docs/MODULES.md` / `schemas/`：三版格式、模块契约与并行扩展说明。
- `scripts/validate-level.cjs`：离线格式及同引擎样例验证入口。
- `js/physics.js`：通用几何规则、刚体构造和试坐判定。
- `js/render.js` / `js/render-art.js`：Canvas分层合成、共用画笔和人物；元素自己绘制外观。
- `js/game.js`：输入、流程、提示、成绩、音效和链接。
- `vendor/matter.min.js` / `vendor/Matter-LICENSE.txt`：Matter.js 0.20.0 及 MIT 许可。
- `scripts/`：零运行依赖的本机静态服务。

后续可按独立namespace继续追加模块和关卡；本轮不含结构断裂、联机、可视化编辑器、模型服务或公网部署。
